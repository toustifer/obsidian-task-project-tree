{
	"name": "obsidian-task-project-tree",
	"version": "0.0.1",
	"description": "Turn folders into task projects with overview notes and tree-based progress.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"task",
		"project",
		"productivity"
	],
	"author": "Codex",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.20.0",
		"obsidian": "^1.6.0",
		"typescript": "^5.3.0"
	}
}
